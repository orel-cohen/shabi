import { Card } from 'antd';
import { useHistory } from 'react-router-dom';

function SendConstraintsPage() {

    return (
        <div>
            שליחת אילוצים
        </div>
    );
}

export default SendConstraintsPage;
